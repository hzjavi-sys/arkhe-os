"use client";
import React from "react";
import AppShell from "../../../components/arkhe/AppShell";

export default function AgentePage() {
  return (
    <AppShell title="Agente (Gente)">
      <div style={{ color: "#0f172a", fontWeight: 800 }}>
        Base lista. Próximo: wizard de creación de proyecto + director + dock inferior del proyecto activo.
      </div>

      <div style={{ marginTop: 12, color: "#334155" }}>
        La “gente” crea webs/apps/sistemas/automatizaciones guiada por el Director (lo conectamos después).
      </div>
    </AppShell>
  );
}
